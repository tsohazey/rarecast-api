services:
  - type: web
    name: megabass-grail-hunter
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python main.py
    autoDeploy: true
    schedule:
      - "*/12 * * * *"  # Runs every 12 minutes (max free frequency)
